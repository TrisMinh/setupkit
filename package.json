{
  "name": "setupkit",
  "version": "0.7.0",
  "description": "Lightweight Windows developer workstation builder with a verified app catalog.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "catalog:verify": "node scripts/check-winget-candidates.js && node scripts/check-winget-expansion.js",
    "catalog:build": "node scripts/build-catalog.js",
    "icons:fetch": "node scripts/fetch-app-icons.js",
    "validate": "npm run catalog:build && node scripts/build-phosphor-subset.js && node --check frontend/renderer.js && node --check frontend/theme-init.js && node scripts/validate-project.js",
    "test": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-native.ps1 -TestOnly",
    "package:win": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-native.ps1"
  }
}
